

# Rename "CAMLY Pool" to "FUN PLAY TREASURY Wallet Balance"

## Changes

### File 1: `src/components/Admin/tabs/RewardPoolTab.tsx` (line 355)
- Change label from `"CAMLY Pool"` to `"FUN PLAY TREASURY Wallet Balance"`

### File 2: `src/components/Layout/EnhancedHonobar.tsx` (line 14)
- Change label from `"CAMLY Pool"` to `"FUN PLAY TREASURY Wallet Balance"`

### File 3: `src/hooks/useHonobarStats.tsx` (line 61)
- Update comment from `"CAMLY Pool"` to `"FUN PLAY TREASURY Wallet Balance"` (cosmetic, keeps code consistent)

These are simple text replacements. No logic changes needed. The updates will apply to both web and mobile since the components are responsive.

